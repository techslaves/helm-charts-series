## **Creating a Chart**

**Create a sample chart using the built-in Helm command.**

```
helm create myapp
```

## **📌 Chart Structure Breakdown**

### **1. Chart.yaml**

This is the metadata file — it stores the chart name, version, description, and app version.

### **2. values.yaml**

It contains the default configuration for the chart — image name, tag, replica count, service port, and more.

Users can override any of these values during deployment.

### **3. templates/ folder**

This is where your actual Kubernetes YAML manifests live.
But instead of static YAML, these are Go templates — dynamic files that take values and render full manifests during installation.

Inside templates you'll find:

- deployment.yaml
- service.yaml
- ingress.yaml
- hpa.yaml
- serviceaccount.yaml
- `_helpers.tpl` (for reusable snippets)

### **4.** .helmignore

Similar to .gitignore — files to ignore while packaging charts.

### **5. .helpers.tpl**

Defines usable Go-template snippets.

Helm loads all files inside the `templates/` directory — including `_helpers.tpl` — during template rendering.

Nothing inside `_helpers.tpl` is rendered into Kubernetes manifests unless you **call** the named templates using `include` or `template`.


---

## **📌 Understanding Templating Basics**

**Helm uses the Go templating engine.
Where you normally hardcode values in YAML, Helm replaces them with `{{ ... }}` expressions.”**

Example:

```
image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
```

**“This means your Deployment automatically picks up whatever values the user provides.”**

---

## **📌 Overriding Values**

**You can override values during installation like this:**

```
helm install myapp ./myapp --set replicaCount=3
```

**“Or with a custom file:”**

```
helm install myapp ./myapp -f values-prod.yaml
```

**“This makes Helm extremely flexible for dev, staging, or production deployments.”**
