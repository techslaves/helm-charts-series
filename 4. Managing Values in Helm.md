## 📌 **Managing Values in Helm**

**This file holds default configurations. But users can override everything.**

Show typical values:

```
replicaCount: 1
image:
  repository: nginx
  tag: stable
service:
  type: NodePort
  port: 80
```

## 📌 Creating Environment-Specific Values

**Environment specific files:**

- values-dev.yaml
- values-prod.yaml

Example `values-prod.yaml`:

```
replicaCount: 3
image:
  tag: "1.2.0"
```

Deployment:

```
helm install myapp ./myapp -f values-prod.yaml
```

## 📌 **Precedence of values in Helm**

**CLI `--set` > values file > default values.yaml**

## 📌 Debugging Values

```
helm get values myapp
helm get manifest myapp
```

## 📌 Handling Secrets

**Never store secrets in plain values.yaml.**

Suggest tools:

- Sealed Secrets
- External Secrets
- SOPS
- Helm Secrets plugin