## 📌 Packaging a Chart

```
helm package myapp
```

Produces:

```
myapp-0.1.0.tgz
```

---

---

## 📌 Creating a helm repository (Hosting on GitHub Pages)

Steps:

1. Create a GitHub repo
2. Create a branch named `*gh-pages*`
3. Clone this in your local
4. Create `charts` folder in **main** branch and push current latest version of charts
5. Package helm chart `helm package myapp`
6. Copy this helm chart to root folder in `gh-pages` branch
7. Run `helm repo index .`  which will create or update index file
8. Push changes to github repository **gh-pages** branch
9. Enable GitHub Pages : Goto Repo Setting → Click on Pages under Code and Automation → Select Deploy from Branch , select branch as `gh-pages` and select folder as `root` → Click on Save. (this will trigger a github actions pipeline to deploy github page)
10. Go to https://<username>.github.io/<repository-name>/

Helm Chart repo is now Ready for use. Happy Helming!!

1. Add this repository
```
helm repo add myrepo https://<user>.github.io/<repo>
helm repo update
helm search repo myrepo
```